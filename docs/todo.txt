
        TO DO


    2018/03/27 05:49 pm
    x Fix moving ignore images up
    x Add current images to the same random list to ignore them
    x Fix delete files
    x Go back to one data stream, instead of separating ignored
    x On file load, verify existence, clean missing files
    x Trash button sometime dissapear

    Deactivate shorcuts while editing

    Extract love ones
    Sort by rank by clicking
    Rename images by order
    Image width auto adjust to fit screen
    Webm support

    Shortcut to go back to the last image
    Button shorcuts for each main part

    Tag by folder
    Tag by image
    Tag cloud below path input

    Delete a recent path entry
    A recent entry that doesn't exists (404) should be removed?
    Show feedback when the files are being saved
    Drag and drop to detect path

    bug Last image card in line, when ignored/unloved the scrollbar stays there


    2018/03/25 05:04 pm
    x Better path names for files
    x Delete real images
    x Delete button on ignored images
    x Icons on all buttons
    x Buttons to the top right next to the image
    x Loved images should be noticeable
    x React code in his own file

    2018/03/13 08:05 pm
    x App files to their own folder inside eelapp
    x Random image when 'r' is pressed
    x Detect current image
    x 'j' Next image
    x 'k' Previous image
    x 'l' love toggle the card
    x 'i' Ignore toggle the card
    x 'n' goes to a random image
    x Detect scrollbar event
    x Love only show love
    x Ignore only show ignore

    2018/03/06 04:06 am
    x Show normal waiting feedback
    x Show loved waiting feedback
    x Show ignored waiting feedback
    x Show total images
    x Show loved count
    x Show ignore count
    x bug Editing the text fucks the ignore category
    x bug Moving up/down needs to ignore true order when filtering by love/ignore
    x Filtering join ignored and love
    x Love button need to fix scroll on unlove
    x Separate ignore from main list
    x Save/load ignore from config
    x Is not neccesary to move the ignored at the end

    2018/02/26 05:54 pm
    x Load recent paths from app config data
    x Save recent paths to app config data
    x Ignore / Love scroll bar to the top
    x bug Images order is overwritten by the loading
    x Ignore button should move the scroll to a better place to avoid weirdness
    x Show scaned folders individually as bookmarks below path input
    x There must be a recent path directory
    x Recent file directory list updates the path field
    x Show loved
    x Love button
    x Update new images

    2018/02/20 07:08 pm
    x Ignored posts can be enabled again with the same button
    x Show ignored posts
    x Save feedback while copying the files
    x Delete / ignore posts
    x Save should create a Qbot file in the original path folder
    x bug When the path is wrong, promises exception isn't detected correctly
    x bug Description need to be updated on file load
    x Load the file data when opening the same path

    2018/02/19 06:41 pm
    x Ignore default description value when saving
    x Reorder posts, up, down button
    x Navbar on top
    x Better folder names
    x Folder names are md5 of the original path flat name
    x Enter key to scan

    2018/02/18 06:01 pm
    x Ctrl+Enter save description
    x Edit data into the data structure
    x A main data structure is required
    x Editable posts field
    x Display cached files as list
    x Copy only images, not the entire thing

    2018/02/13 11:14 pm
    x Copy files into a cache
    x Display message when files not found
    x Filter files with a list of words
